const SectionLabel = ({ children }) => (
  <div className="mx-auto mb-6 w-fit border-2 border-current px-6 py-2">
    <span className="text-sm font-bold tracking-[0.2em]">{children}</span>
  </div>
);

export default SectionLabel;